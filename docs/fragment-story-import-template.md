---
# 可选：整条 Fragment 故事共用的标签。
# 类型：自由文本列表；默认：空列表。
# 章节可以在各自的 Markdown 中另外配置自己的 tags。
tags: []
# tags:
#   - 校园
#   - 林越
---

从这里开始填写整条 Fragment 故事的总览，支持普通 Markdown。

本文件导入时必须命名为 `_story.md`，并放在 `fragments/<故事名>/` 目录中。故事标题来自目录名，不是 `_story`。
