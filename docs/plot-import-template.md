---
# 必填：正式章号。
# 类型：整数；范围：1～99999。
chapterNumber: 1

# 可选：所属故事列表；默认只有“主线”。
# 类型：自由文本列表；至少填写一个非空名称。
# 普通 Plot 通常只属于一个故事；交叉剧情可以同时属于多个故事。
# 建议只写核心名称，例如“东港”；Story Teller 会按页面显示为“东港篇”或“东港线”。
# stories 与 tags 相互独立。
stories:
  - 主线
# stories:
#   - 主线
#   - 东港

# 可选：剧情摘要。
# 类型：自由文本；默认：空。
summary: ''
# summary: 在这里填写剧情摘要。

# 可选：剧情状态。
# 只能选择：素材、草稿、待串联、已接入、已完成。
# 默认：草稿。
status: 草稿

# 可选：标签列表；每一项都是自由文本。
# tags 与 story 相互独立。
tags: []
# tags:
#   - 资金案
#   - 江家

# 可选：是否为关键剧情。
# 只能填写 YAML 布尔值 true 或 false；默认：false。
key: false

# 可选：是否为高潮剧情。
# 只能填写 YAML 布尔值 true 或 false；默认：false。
climax: false
---

从这里开始填写正式剧情正文，支持普通 Markdown。

文件名去掉 `.md` 后就是 Plot 标题。
